import './party-popper.module.scss';

/* eslint-disable-next-line */
export interface PartyPopperProps {}

export function PartyPopper(props: PartyPopperProps) {
  return ( <span>🎉🎉🎉</span>);
}

export default PartyPopper;
